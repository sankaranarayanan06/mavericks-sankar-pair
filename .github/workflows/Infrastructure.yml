on:
  push:
    branches:
      - infra-main

jobs:
  terraform:
    name: "Terraform"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Terraform Format
        uses: hashicorp/setup-terraform@v1
        run :
            cd infrastructure
            terraform fmt




